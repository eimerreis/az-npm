# Changesets

Use `bun run changeset` to record release intent.

Use `bun run version-packages` to apply pending changesets locally.

The GitHub release workflow will publish to npm and create a GitHub release when a versioned release is merged to `main`.
